[{{ range $index, $page := .Site.RegularPages }}{{ if $index }},{{ end }}
  {"title": {{ $page.Title | jsonify }},"permalink": {{ $page.Permalink | jsonify }},"summary": {{ $page.Summary | jsonify }},"content": {{ $page.Plain | jsonify }}}{{ end }}
]
